# Nostr secret key for the dedicated Buzz bridge identity (64 lowercase hex characters).
BUZZ_PRIVATE_KEY=

# Matrix access token for the @buzzbridge account after that account logs into Slack.
MATRIX_ACCESS_TOKEN=

# Optional. Defaults to ./config.json.
BRIDGE_CONFIG=./config.json
